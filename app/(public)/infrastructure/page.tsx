import Link from "next/link";
import { ArrowUpRight, Building2, FlaskConical } from "lucide-react";
import { EmptyState, PageHeader, SectionHeading, SourceNote } from "@/components/ui";
import { getSiteData } from "@/lib/store";

export const metadata = { title: "Infrastructure", description: "IET-DSMNRU infrastructure and laboratory discovery surface." };

export default async function InfrastructurePage() { const data = await getSiteData(); return <><PageHeader eyebrow="About IET" title="Infrastructure you can discover, not just admire." description="IET infrastructure is represented through structured facility records, images, documents and department relationships." breadcrumbs={[{ label: "Infrastructure" }]} /><section className="section"><div className="container"><SectionHeading eyebrow="Facility register" title="Published laboratory headings." href="/laboratories" linkLabel="All laboratories" /><div className="data-grid">{data.laboratories.map((lab) => <Link href={`/laboratories/${lab.slug}`} className="data-card" key={lab.id}><FlaskConical size={24} color="var(--copper)" /><h3>{lab.name}</h3><p>{lab.departmentName}</p><span className="link-arrow">Open record</span></Link>)}</div><div style={{ marginTop: 28 }}><SourceNote>The supplied PDF includes laboratory and workshop headings but not an approved infrastructure inventory. Equipment, capacity and accessibility data must be entered through the CMS.</SourceNote></div></div></section><section className="section soft"><div className="container content-grid"><div><Building2 size={27} color="var(--copper)" /><h2 style={{ marginTop: 15 }}>Campus context</h2><p className="lead">The IET profile describes laboratories, workshops and smart classrooms as part of the institute's academic base. The platform keeps infrastructure records connected to programmes, departments and research.</p></div><div className="data-card"><h3>Upload approved facility media</h3><p className="small">Images and PDFs can be uploaded through the media and document CMS with alt text, captions and storage metadata.</p><Link className="link-arrow" href="/admin/content/media">Open media library <ArrowUpRight size={14} /></Link></div></div></section></>; }
