import Link from "next/link";
import { ExternalLink, ShieldCheck } from "lucide-react";
import { PageHeader, SectionHeading } from "@/components/ui";
import { getSiteData } from "@/lib/store";

export const metadata = { title: "Admissions", description: "Explore IET programmes and find official DSMNRU admission information." };

export default async function AdmissionsPage() { const data = await getSiteData(); return <><PageHeader eyebrow="Admissions" title="Discover IET. Apply through DSMNRU." description="Explore IET programmes and departments, then check DSMNRU for current application dates, eligibility, fees and forms." breadcrumbs={[{ label: "Admissions" }]} /><section className="section"><div className="container"><div className="content-grid"><div><SectionHeading eyebrow="Applying to IET" title="Admission information" description="Consult the current university admission bulletin for eligibility, fees, application dates and available seats." /><div className="data-card" style={{ background: "var(--ink)", color: "white", borderColor: "var(--ink)" }}><ShieldCheck size={28} color="var(--lime)" /><h3 style={{ color: "white", marginTop: 17 }}>Official DSMNRU admissions</h3><p style={{ color: "#b9c7ce" }}>Use the official DSMNRU admissions service for application process, current bulletins, eligibility, fees and university notices.</p><a className="link-arrow" style={{ color: "var(--lime)" }} href="https://dsmru.up.nic.in/main/User/admission_pro.aspx" target="_blank" rel="noreferrer">Open official admissions <ExternalLink size={14} /></a></div></div><aside className="info-aside"><div className="aside-card"><h3>Where to find information</h3><div className="aside-item"><strong>IET provides</strong>Programme information, departments, faculty and laboratories.</div><div className="aside-item"><strong>DSMNRU provides</strong>Applications, eligibility rules, fees, forms, examinations and results.</div><div className="aside-item"><strong>Verify before relying</strong>Seat matrix and dates can change; confirm every current value with the university.</div></div></aside></div></div></section><section className="section soft"><div className="container"><SectionHeading eyebrow="Programmes" title="Explore your options" href="/programs" linkLabel="All programmes" /><div className="data-grid">{data.programs.slice(0, 6).map((program) => <Link href={`/programs/${program.slug}`} className="data-card" key={program.id}><span className="tag">{program.level} · {program.duration}</span><h3>{program.shortTitle || program.title}</h3><p>{program.departmentName}</p><span className="link-arrow">View programme</span></Link>)}</div></div></section></>; }
