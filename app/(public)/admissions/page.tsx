import Link from "next/link";
import { ArrowUpRight, ExternalLink, ShieldCheck } from "lucide-react";
import { PageHeader, SectionHeading, SourceNote } from "@/components/ui";
import { getSiteData } from "@/lib/store";

export const metadata = { title: "Admissions", description: "IET programme discovery with clear hand-off to official DSMNRU admissions infrastructure." };

export default async function AdmissionsPage() { const data = await getSiteData(); return <><PageHeader eyebrow="Admissions" title="Discover IET. Apply through DSMNRU." description="This website explains IET programmes and departments; the university owns current application windows, eligibility, fees and admission forms." breadcrumbs={[{ label: "Admissions" }]} /><section className="section"><div className="container"><div className="content-grid"><div><SectionHeading eyebrow="Boundary by design" title="No duplicated admissions rules." description="The supplied IET profile includes a seat matrix and programme durations, but not the full current eligibility or fee rules. Those values are not fabricated here." /><div className="data-card" style={{ background: "var(--ink)", color: "white", borderColor: "var(--ink)" }}><ShieldCheck size={28} color="var(--lime)" /><h3 style={{ color: "white", marginTop: 17 }}>Official source of truth</h3><p style={{ color: "#b9c7ce" }}>Use the official DSMNRU admissions service for application process, current bulletins, eligibility, fees and university-owned notices.</p><a className="link-arrow" style={{ color: "var(--lime)" }} href="https://dsmru.up.nic.in/main/User/admission_pro.aspx" target="_blank" rel="noreferrer">Open official admissions <ExternalLink size={14} /></a></div></div><aside className="info-aside"><div className="aside-card"><h3>Admission hand-off</h3><div className="aside-item"><strong>IET provides</strong>Programme discovery, department context, faculty and laboratory relationships.</div><div className="aside-item"><strong>DSMNRU provides</strong>Application infrastructure, eligibility rules, fees, forms, examination and results.</div><div className="aside-item"><strong>Verify before relying</strong>Seat matrix and dates can change; confirm every current value with the university.</div></div><SourceNote>The official DSMNRU website lists Admission Process, UG/PG prospectuses and Fees under Admissions. IET should link to those services rather than copy them.</SourceNote></aside></div></div></section><section className="section soft"><div className="container"><SectionHeading eyebrow="Published programme records" title="Choose a programme to understand its context." href="/programs" linkLabel="All programme records" /><div className="data-grid">{data.programs.slice(0, 6).map((program) => <Link href={`/programs/${program.slug}`} className="data-card" key={program.id}><span className="tag">{program.level} · {program.duration}</span><h3>{program.shortTitle || program.title}</h3><p>{program.departmentName}</p><span className="link-arrow">View programme</span></Link>)}</div></div></section></>; }
