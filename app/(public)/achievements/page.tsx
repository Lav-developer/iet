import { Award, ExternalLink } from "lucide-react";
import { EmptyState, PageHeader, SectionHeading, SourceNote } from "@/components/ui";
import { getSiteData } from "@/lib/store";

export const metadata = { title: "Achievements", description: "A structured IET-DSMNRU achievement archive for students, teams, researchers and departments." };

export default async function AchievementsPage() { const data = await getSiteData(); return <><PageHeader eyebrow="Campus life" title="Evidence of people doing the work." description="Achievements can be filtered by category, year, department and recipient once approved records are entered into the CMS." breadcrumbs={[{ label: "Achievements" }]} /><section className="section"><div className="container"><SectionHeading eyebrow="Achievement archive" title="No invented trophies." description="The source PDF does not provide an approved achievement list, so the public view stays honest and ready for records." />{data.achievements.length ? <div className="data-grid">{data.achievements.map((achievement) => <div className="data-card" key={achievement.id}><Award size={23} color="var(--copper)" /><span className="tag" style={{ marginTop: 17 }}>{achievement.category}</span><h3>{achievement.title}</h3><p>{achievement.description}</p></div>)}</div> : <EmptyState title="No achievements published yet" description="Add a verified student, team, research, innovation or academic achievement through the CMS." />}<div style={{ marginTop: 28 }}><SourceNote>Achievements must be supported by an approved record and, where relevant, associated media or document. No ranking, award or placement claim is inferred from the supplied source.</SourceNote></div></div></section><section className="section soft"><div className="container"><div className="content-grid"><div><div className="eyebrow">Suggested record fields</div><h2>Make the achievement findable.</h2><p className="lead">Category · student/team · department · year · event · description · source document · media · publication status.</p></div><div className="data-card"><h3>Admin entry point</h3><p className="small">Editors can create a draft, attach media and submit it for review. The public page only renders published records.</p><a href="/admin/content/achievements" className="link-arrow">Open achievement CMS <ExternalLink size={14} /></a></div></div></div></section></>; }
