import { FlaskConical } from "lucide-react";
import { LaboratoryDirectory } from "@/components/laboratory-directory";
import { PageHeader, SectionHeading, SourceNote } from "@/components/ui";
import { getSiteData } from "@/lib/store";

export const metadata = { title: "Laboratories", description: "Explore IET-DSMNRU laboratory records and connect facilities to departments, programmes and research." };

export default async function LaboratoriesPage() { const data = await getSiteData(); return <><PageHeader eyebrow="Facilities" title="Laboratories as first-class assets." description="Discover facility records by department, then connect them to programmes, research relevance, equipment, people and projects as the CMS is populated." breadcrumbs={[{ label: "Laboratories" }]} /><section className="section"><div className="container"><SectionHeading eyebrow="Facility discovery" title="What is published today." description="The supplied profile includes several laboratory and workshop headings. Detailed equipment inventories remain explicitly marked for CMS entry." /><LaboratoryDirectory laboratories={data.laboratories} departments={data.departments} /><div style={{ marginTop: 28 }}><SourceNote>Laboratory names are based on the headings and lab-instructor references in the supplied IET-DSMNRU profile PDF. Equipment, capacity, access and image galleries require an approved CMS record.</SourceNote></div></div></section><section className="section dark"><div className="container content-grid"><div><div className="eyebrow inverse">Facility model</div><h2>A lab can explain how a programme becomes practice.</h2><p className="lead">Each laboratory record has structured space for equipment, supported courses, research relevance, associated faculty, projects and gallery media. That relationship graph is the point — not a decorative card wall.</p></div><div className="data-card" style={{ background: "#13263a", borderColor: "#2a4a62", color: "white" }}><FlaskConical size={30} color="var(--lime)" /><h3 style={{ marginTop: 20, color: "white" }}>CMS ready</h3><p style={{ color: "#b9c7ce" }}>Add a facility once; it can surface on department, programme, faculty and research pages without duplicating copy.</p></div></div></section></>; }
