import Link from "next/link";
import { Accessibility, Check, Keyboard, TextCursorInput } from "lucide-react";
import { PageHeader, SectionHeading, SourceNote } from "@/components/ui";

export const metadata = { title: "Accessibility", description: "Accessibility architecture and contact guidance for the IET-DSMNRU information platform." };

export default function AccessibilityPage() { return <><PageHeader eyebrow="Accessibility" title="Accessibility is part of the system." description="The platform is designed so academic information remains usable on keyboard, mobile, screen readers and different visual preferences." breadcrumbs={[{ label: "Accessibility" }]} /><section className="section"><div className="container content-grid"><div><SectionHeading eyebrow="Built in" title="A preference panel is only one layer." description="Semantic structure, content governance and media metadata are treated as product requirements, not a visual afterthought." /><div className="data-grid"><Feature icon={<Keyboard />} title="Keyboard ready" text="Skip link, visible focus states, logical headings, labelled forms and navigable dialogs." /><Feature icon={<TextCursorInput />} title="Readable by design" text="Responsive type, high contrast option, text resizing and restrained motion." /><Feature icon={<Accessibility />} title="Content discipline" text="Alt text, captions, accessible PDFs and structured tables are fields in the CMS model." /><Feature icon={<Check />} title="Reduced motion" text="The preference panel can reduce non-essential transitions and animations." /></div></div><aside className="info-aside"><div className="aside-card"><h3>Try the controls</h3><p className="small">Use the accessibility icon in the header to change text size, contrast, reduced motion or reset preferences.</p><div className="aside-item"><strong>For editors</strong>Never publish an image or document without meaningful alternative text / title metadata.</div><div className="aside-item"><strong>For review</strong>Test every new content type with keyboard navigation and a screen reader before publishing.</div></div><SourceNote>Institutional accessibility requirements should be reviewed and approved by DSMNRU / IET before launch.</SourceNote></aside></div></section><section className="section soft"><div className="container"><div className="data-card"><h3>Report an accessibility barrier</h3><p className="small">This prototype does not invent a grievance or accessibility email. Add the approved contact through the CMS once IET designates the channel.</p><Link href="/admin/content/contacts" className="link-arrow">Add an approved contact</Link></div></div></section></>; }
function Feature({ icon, title, text }: { icon: React.ReactNode; title: string; text: string }) { return <div className="data-card"><span style={{ color: "var(--copper)" }}>{icon}</span><h3 style={{ marginTop: 17 }}>{title}</h3><p>{text}</p></div>; }
