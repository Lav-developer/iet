"use client";

import Link from "next/link";
import { ShieldCheck, UserPlus, UserX } from "lucide-react";
import { useEffect, useState } from "react";

type UserRecord = { id: string; email: string; name: string; role: string; departmentId?: string | null; active: boolean; sessionVersion: number; department?: { slug: string; name: string } | null };

export default function UsersPage() {
  const [users, setUsers] = useState<UserRecord[]>([]);
  const [form, setForm] = useState({ email: "", name: "", password: "", role: "EDITOR", departmentId: "" });
  const [error, setError] = useState("");
  const [message, setMessage] = useState("");
  const [loading, setLoading] = useState(true);
  const load = () => { setLoading(true); fetch("/api/admin/users").then(async (response) => { const body = await response.json(); if (!response.ok) throw new Error(body.error || "Unable to load users"); setUsers(body.users || []); }).catch((err) => setError(err.message)).finally(() => setLoading(false)); };
  useEffect(load, []);
  const create = async (event: React.FormEvent) => { event.preventDefault(); setError(""); setMessage(""); const response = await fetch("/api/admin/users", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...form, departmentId: form.departmentId || undefined }) }); const body = await response.json().catch(() => ({})); if (!response.ok) { setError(body.error || "Unable to create user"); return; } setForm({ email: "", name: "", password: "", role: "EDITOR", departmentId: "" }); setMessage("Administrator created."); load(); };
  const deactivate = async (id: string) => { if (!window.confirm("Deactivate this account and invalidate its sessions?")) return; const response = await fetch("/api/admin/users", { method: "DELETE", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ id }) }); const body = await response.json().catch(() => ({})); if (!response.ok) { setError(body.error || "Unable to deactivate user"); return; } setMessage("Account deactivated and active sessions invalidated."); load(); };
  return <><div className="entity-toolbar"><div><div className="admin-breadcrumb">Governance / User administration</div><h2>Administrator accounts</h2><p className="small">Manage named accounts, department scope and explicit session invalidation. Passwords are never displayed.</p></div><Link href="/admin/audit" className="button secondary small-button">Audit log</Link></div>{error && <div className="alert" role="alert">{error}</div>}{message && <div className="success" role="status">{message}</div>}<div className="admin-panel"><div className="admin-panel-head"><h3><UserPlus size={17} /> Create administrator</h3><span className="tag"><ShieldCheck size={13} /> Named access only</span></div><form className="form-grid" onSubmit={create}><div className="form-field"><label htmlFor="user-name">Name</label><input id="user-name" className="form-control" required value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} /></div><div className="form-field"><label htmlFor="user-email">Email</label><input id="user-email" className="form-control" type="email" required value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} /></div><div className="form-field"><label htmlFor="user-password">Temporary password</label><input id="user-password" className="form-control" type="password" minLength={12} required value={form.password} onChange={(event) => setForm({ ...form, password: event.target.value })} /><span className="form-hint">At least 12 characters. Share through an approved channel and rotate after first sign-in.</span></div><div className="form-field"><label htmlFor="user-role">Role</label><select id="user-role" className="form-select" value={form.role} onChange={(event) => setForm({ ...form, role: event.target.value })}><option>EDITOR</option><option>DEPARTMENT_ADMIN</option><option>IET_ADMIN</option><option>SUPER_ADMIN</option></select></div><div className="form-field"><label htmlFor="user-department">Department ID (for department admin)</label><input id="user-department" className="form-control" value={form.departmentId} onChange={(event) => setForm({ ...form, departmentId: event.target.value })} /><span className="form-hint">Use the normalized Department ID; leave blank for institute-wide roles.</span></div><div className="form-actions full"><button className="button small-button" type="submit">Create account</button></div></form></div><div className="admin-panel" style={{ padding: 0, overflowX: "auto" }}><table className="entity-table"><thead><tr><th>Account</th><th>Role</th><th>Scope</th><th>Sessions</th><th>Status</th><th>Action</th></tr></thead><tbody>{loading ? <tr><td colSpan={6}>Loading accounts…</td></tr> : users.map((user) => <tr key={user.id}><td><strong>{user.name}</strong><br /><span className="small">{user.email}</span></td><td><span className="status-pill review">{user.role.replaceAll("_", " ")}</span></td><td>{user.department?.name || "Institute-wide"}</td><td>{user.sessionVersion}</td><td>{user.active ? <span className="status-pill published">Active</span> : <span className="status-pill archived">Inactive</span>}</td><td>{user.active && <button className="mini-button danger" onClick={() => deactivate(user.id)}><UserX size={13} /> Deactivate</button>}</td></tr>)}</tbody></table></div></>;
}
